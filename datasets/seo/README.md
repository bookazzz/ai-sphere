# SEO research datasets

These CSV/XLSX files are offline keyword and competitor research inputs. They are not imported by the Next.js or FastAPI runtime and must not be copied into production deployment artifacts.

Only `out/`, `backend/`, the production environment file, and reverse-proxy configuration are required for deployment.
